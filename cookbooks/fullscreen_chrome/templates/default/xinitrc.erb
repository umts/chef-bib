!#/bin/sh

while true; do
  killall -TERM chromium 2>/dev/null;
  killall -TERM ratpoison 2>/dev/null;
  sleep 2;
  killall -9 chromium 2>/dev/null;
  killall -9 ratpoison 2>/dev/null;

  xset -dpms
  xset s off

  /usr/bin/chromium --app='<%= @url -%>' <%= @options -%> &
  exec ratpoison
done;
